{
  "name": "main",
  "version": "1.0.0",
  "description": "Main entrypoint for electron runtime.",
  "main": "./src/index.js",
  "scripts": {
    "electron": "electron src",
    "start": "set NODE_ENV=dev && nodemon --watch ./src/**/* --watch . --exec \"npm run electron\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.3.1",
    "nodemon": "^2.0.15"
  }
}
